export class UploadTransformObj {
	/**
	 * Transform a stream.
	 * @param {stream.Readable} file The file content to transform
	 * @return {stream.Readable|Buffer} The transformed content
	 */
	// async processFile(file) { }
}
